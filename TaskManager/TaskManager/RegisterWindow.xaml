<Window x:Class="TaskManager.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManager"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="500" Width="400"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="30">
            <Label Content="Login" />
            <TextBox x:Name="LoginTextBox" />
            <Label Content="Password" />
            <TextBox x:Name="PasswordTextBox" />
            <Label Content="Password repeat" />
            <TextBox x:Name="PasswordRepeatTextBox" />
            <Label Content="First name" />
            <TextBox x:Name="FirstNameTextBox" />
            <Label Content="Last name" />
            <TextBox x:Name="LastNameTextBox" />
            <Button Content="Register" Margin="80,20" Click="RegisterButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
